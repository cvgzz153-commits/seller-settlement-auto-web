# 정적 앱 검증 기록

2026-08-12 13:10 GMT+9에 로컬 프리뷰를 확인한 결과, 화면 렌더링과 샘플 체크박스 선택은 정상 동작했다. 그러나 샘플 정산 실행 시 `엑셀 처리 라이브러리를 불러오지 못했습니다` 오류가 표시되었다. 원인은 외부 SheetJS CDN 스크립트의 SRI 무결성 해시가 실제 응답과 일치하지 않아 브라우저가 로드를 차단했을 가능성이 높다. 다음 단계에서 무결성 속성을 제거하거나 유효한 로컬 번들을 사용해 라이브러리 로딩을 복구한 뒤, 정산·엑셀 다운로드 흐름을 다시 검증한다.

수정 후 재검증에서 SheetJS 라이브러리가 정상 로드되었다. 샘플 테스트 선택 후 정산을 실행하자 정상 입금 3명, 입금 누락/불일치 1명으로 결과가 생성되었으며 `박민수 / 15,000원 / 일치하는 입금 기록 없음`이 표에 표시되고 누락자 명단 엑셀 다운로드 버튼도 노출되었다.

GitHub Pages REST API 활성화는 현재 GitHub 통합 토큰 권한으로 403 `Resource not accessible by integration`이 반환되어 자동 게시할 수 없었다. jsDelivr CDN 주소는 HTML을 원문 텍스트로 제공해 단독 웹 앱 URL로 사용할 수 없었고, raw.githack 경로는 최초 접근 시 외부 콘텐츠 주의 페이지를 표시했다. 정적 앱 소스 저장소는 공개 상태이며, 이를 실제 웹 페이지로 제공하는 추가 자동 호스팅 경로가 필요하다.

raw.githack 공개 URL의 안내 화면을 통과한 뒤, 정산 앱이 실행 가능한 웹페이지로 렌더링되었다. 페이지 컨텍스트에서 확인한 결과 `runButton`이 존재하고 SheetJS `XLSX` 객체도 정상 로드되었다.

공개 URL에서 샘플 정산을 실행한 결과, 정상 입금 3명, 누락/불일치 1명, `박민수 / 15,000원 / 일치하는 입금 기록 없음` 행, 다운로드 버튼 표시를 확인했다. 최종 공개 주소로 `https://htmlpreview.github.io/?https://github.com/cvgzz153-commits/seller-settlement-auto-web/blob/main/index.html`을 확인했으며, 중간 클릭 없이 정적 앱을 즉시 렌더링한다.

로컬 SheetJS 번들을 포함한 커밋 `c5c0d11466adc18f44ef2f42145389c966dda625` 기반의 HTMLPreview URL도 중간 안내 없이 앱 화면을 즉시 표시했다. 이 URL에서 번들 로드와 샘플 정산을 별도로 확인한다.

최종 URL에서 확인한 결과 로컬 SheetJS 번들이 정상 로드되었고, 샘플 정산은 정상 입금 3명·누락/불일치 1명을 반환했다. 누락자 행은 `박민수 / 15,000원 / 일치하는 입금 기록 없음`으로 표시됐으며 엑셀 다운로드 버튼도 활성화됐다.

무료 베타 화면, 매칭 기준 선택, 피드백 이슈 링크, 로컬 SheetJS 번들은 모두 정상 로드되었다. 다만 샘플 데이터가 행마다 서로 다른 헤더 키를 사용하도록 작성되어, 첫 번째 시트에서 헤더가 하나로 고정되는 실제 엑셀 구조와 달리 자동 열 인식이 왜곡되었다. 샘플 데이터를 주문서와 입금 내역 각각 동일한 헤더 체계로 정리한 뒤 주문번호·연락처 뒷자리·이름 매칭 결과를 재검증한다.

샘플 데이터를 실제 엑셀처럼 고정 헤더 구조로 정리한 뒤 재검증했다. 주문서의 `구매자·주문금액·연락처·주문번호`와 입금 내역의 `입금자명·입금금액·입금자 연락처·입금메모`를 자동 인식했으며, 주문번호·연락처 뒷자리·이름 매칭으로 정상 3명, 확인 필요 1명(박민수 / 15,000원)을 반환했다. 베타 피드백 버튼도 새 이슈 작성 주소를 정확히 가리킨다.

토스 계열의 밝은 블루 포인트, 넉넉한 여백, 브랜드 상단 바, 계층화된 카드, 정제된 업로드 영역과 CTA 버튼으로 UI를 전면 개편했다. 로컬 브라우저에서 화면 렌더링을 확인했고, 디자인 변경 후에도 샘플 정산은 정상 3명·확인 필요 1명 결과를 반환하며 결과 영역과 피드백 링크가 모두 노출됐다.

멀티 탭 구조의 초기 입금 정산 화면을 확인했고, 배송·알림 탭을 선택하면 기존 정산 화면 대신 템플릿 카드·고객 이름 입력·안내 유형 선택·문구 생성 버튼이 정상 표시됐다. 탭 전환 시 불필요한 서버 요청이나 파일 전송은 발생하지 않는다.

배송·알림 탭에서 고객 이름 `김하늘`과 입금 확인 유형을 입력해 검증한 결과, `김하늘님, 입금이 정상적으로 확인되었습니다`로 시작하는 개인화 안내 문구가 브라우저에서 정상 생성됐다.

매출 리포트 탭의 기본 대시보드 화면을 확인한 뒤 샘플 정산을 실행했다. 주문 합계 150,000원, 정상 매칭 3건, 확인 필요 1건 및 해당 상태 메시지가 브라우저 내 로컬 집계로 정확히 반영됐고, 활성 탭도 `panel-report`로 정상 전환됐다.

최신 공개 URL에서 초기 렌더링을 재확인했다. 프리미엄 카드형 UI와 입금 정산·배송/알림·매출 리포트 탭이 모두 표시됐고, 파일을 브라우저 안에서만 분석한다는 안내도 정상 노출됐다.

공개 앱에서 샘플 정산을 수행한 뒤 매출 리포트를 확인했으며 주문 합계 150,000원, 정상 매칭 3건, 확인 필요 1건이 반영됐다. 이어 배송·알림 탭으로 전환해 템플릿 생성 제어가 존재하는 것도 확인했다.

상용 전환 기반을 추가한 뒤 로컬 화면에서 우측 하단 고정 문의 버튼이 정상 표시되고 GitHub 이슈 작성 주소로 연결되는 것을 확인했다. 결제 모듈은 Node 문법 검사와 자동 안전 테스트를 통과했으며, 서버가 생성한 월간 요금제 금액 4,900원, 결제 운영 비활성화 상태의 503 응답, 웹훅 수신 200 응답을 확인했다.

구독형 모듈 추가 후 로컬 브라우저에서 샘플 정산을 실행해 기존 자동 열 인식과 3단계 매칭 결과가 정상 매칭 3명·확인 필요 1명으로 유지됨을 확인했다. 매출 리포트 탭은 주문 합계 150,000원, 추정 순수익 105,000원(원가율 30%), 미수금 15,000원, 정산 완료율 75.0% 및 실행 기록 차트를 표시했다. 배송·알림 탭의 자동 발송 버튼과 매출 리포트의 Pro 수익 리포트 버튼은 각각 목적에 맞는 Pro 구독 모달을 정상 표시했다. 결제·구독 권한·알림 모듈의 Node 문법 및 자동 안전 테스트도 통과했다.

Vercel 운영 도메인 `https://settlemate-vert.vercel.app`에서 무료 베타 배너, 파일 비전송 안내, 3개 탭, 우측 하단 문의 버튼이 정상 표시됐다. 샘플 정산은 정상 매칭 3명·확인 필요 1명과 누락자 표·엑셀 다운로드 제어를 생성했다. 배송·알림 탭의 안내 템플릿과 Pro 자동 알림 모듈, 매출 리포트의 주문 합계 150,000원·추정 순수익 105,000원·미수금 15,000원·완료율 75.0%·추세 차트 및 Pro 리포트 진입점을 운영 환경에서 확인했다.

최종 운영 URL: https://settlemate-vert.vercel.app. Vercel에서 GitHub `main` 브랜치를 연결한 자동 배포를 확인했다. 운영 도메인에서 PWA 매니페스트 `https://settlemate-vert.vercel.app/manifest.webmanifest`와 서비스 워커 등록(scope `https://settlemate-vert.vercel.app/`)을 확인했으며, `POST /api/payments/create-order`는 월간 요금제 4,900원과 성공·실패 복귀 URL을 HTTP 200으로 생성했다. 모바일 390×844 렌더링에서 제목 한국어 줄바꿈을 개선해 GitHub 커밋 `11b2bb1`을 푸시했고 Vercel 자동 배포 후 운영 페이지의 최신 화면을 재확인했다.

최신 390×844 모바일 렌더링에서 제목은 '인스타 공구·마켓 정산 / 자동화'로 자연스럽게 두 줄 처리됐고, 무료 베타 배너, 3개 탭, 개인정보 안내 카드, 통계 카드, 고정 문의 버튼이 화면 폭 안에서 겹침이나 가로 잘림 없이 표시됐다.

커밋 `5f1e89a`의 Vercel 자동 배포를 캐시 우회 URL에서 확인했다. 무료 체험은 기본 정산 흐름 밖의 접이식 '예시 데이터로 30초 체험하기' 영역으로 이동했고, 기존 외부 GitHub 피드백 링크와 '공개 이슈 페이지' 문구는 제거됐다. 운영 페이지에서 '별점과 한줄평 남기기' 버튼을 누르면 별점 1~5점과 한줄평 입력을 갖춘 앱 내 리뷰 모달이 열리며, 별점 선택·문구 입력·로컬 저장 완료 안내를 확인했다.

커밋 `711c633`의 Vercel 자동 배포를 캐시 우회 URL에서 확인했다. 운영 페이지 제목은 `SELLERHELP · 무료 베타`로 갱신됐고, 상단에는 시안 D의 정산 문서·체크 심볼과 SELLERHELP 세리프 워드마크·한글 태그라인이 정상 표시됐다. 390×844 모바일 렌더링에서도 로고, 앱 설치 버튼, Free Beta 배지가 서로 겹치지 않고 가독성을 유지했다.

수익화 UI 로컬 검증: 상단 `Free · 요금제` 버튼과 사용량 카드가 표시되며, 현재 Free 플랜과 3회 중 사용 횟수를 확인할 수 있다. 요금제 모달에서 Free와 월 4,900원 SELLERHELP Pro의 기능 차이가 표시되고, Pro 버튼은 기존 안전한 결제 준비 모달로 연결된다. 결제 준비 버튼은 실제 청구 없이 결제 키·Vercel 환경 변수 설정 안내를 표시했다.

수익화 전환 검증: Free·Pro 요금제 비교 모달과 Pro 결제 준비 흐름이 정상 표시됐다. 샘플 정산 회귀 결과는 정상 매칭 3명·확인 필요 1명이며, 무료 사용량은 2 / 3회로 정상 갱신됐다. `npm run check`와 `node test_commercial_api.js`를 통과했고, 후자는 `COMMERCIAL_API_SAFETY_CHECK_OK`를 반환했다. `npm test` 명령은 package.json에 정의되어 있지 않아 실행하지 않았다.

커밋 `dd66f51`의 Vercel 자동 배포를 확인했다. 운영 환경에서 Free 플랜·3회 사용량 카드와 `Free · 요금제` 진입점이 표시됐고, 요금제 모달은 Free와 월 4,900원 SELLERHELP Pro의 기능 비교 및 Pro 구독 준비 버튼을 정상 제공했다. 실제 결제·자동 청구가 연결되지 않았다는 안내도 함께 노출됐다.

## 복사·붙여넣기 및 모바일 입력 모드 검증

- 주문서 표 2행과 입금 내역 표 3행을 탭 구분 형식으로 붙여넣어 열 자동 인식과 정산 실행을 검증했다.
- 주문번호 매칭 1건과 이민준의 10,000원·15,000원 합산입금 후보 1건이 정상 감지됐다.
- 합산입금 확인 상태를 저장한 뒤 검토 완료 1건, 확인 필요 0건, 남은 차액 0원으로 갱신되는 것을 확인했다.
- 파일 업로드 모드와 표 복사·붙여넣기 모드는 서로 전환되며 선택하지 않은 입력 영역은 숨겨지는 것을 확인했다.
- 390px 너비 모바일 렌더링에서 브랜드·탭·입력 모드 안내가 가로 넘침 없이 표시되는 것을 확인했다.
- `npm run check` 및 `node test_commercial_api.js`를 통과했다.
